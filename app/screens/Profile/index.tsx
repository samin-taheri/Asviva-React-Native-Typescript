import React from 'react';
import AppProfile from '@/components/Common/AppProfile';

const Profile = () => {

    return (
        <React.Fragment>
            <AppProfile />
        </React.Fragment>
    );
};

export default Profile;
